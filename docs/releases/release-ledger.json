{
    "schemaVersion": 1,
    "updatedAt": "2026-09-22",
    "releaseOwner": "release-captain",
    "sourceRepository": {
        "name": "obsidian-livesync",
        "branch": "codex-2.0.6-stabilize",
        "commit": "9b2af60d026a049effcb0071ae12fafff5a17d4f"
    },
    "distributionRepository": {
        "name": "3yesore/OsyC",
        "remote": "https://github.com/3yesore/OsyC.git",
        "branch": "main",
        "commit": "9b2af60d026a049effcb0071ae12fafff5a17d4f"
    },
    "currentStable": {
        "version": "2.0.19",
        "tag": "2.0.19",
        "status": "published",
        "channel": "stable",
        "sourceBranch": "release/2.0.19",
        "sourceCommit": "334de7f",
        "assetSourceCommit": "334de7f",
        "releaseCommit": "92514bc",
        "releaseRunId": "35694028272",
        "publishedAt": "2026-09-22T06:15:40Z",
        "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.19",
        "deviceAcceptance": "pending-desktop",
        "assets": [
            "main.js",
            "manifest.json",
            "manifest-beta.json",
            "styles.css",
            "versions.json"
        ],
        "assetSha256": {
            "main.js": "1b89822aa199b93a372f2babc2bf4aad73963007930b48a2224244b0300739de",
            "manifest.json": "770f98c0e5aa06c3c4f72656a2f9faa790cd6f5bc78320abe2ab2feceb91b80a",
            "manifest-beta.json": "770f98c0e5aa06c3c4f72656a2f9faa790cd6f5bc78320abe2ab2feceb91b80a",
            "styles.css": "cbc4e6da4f47579ce5158a75e5e96c275f107423b3b201c296d509aafd6f03fa",
            "versions.json": "ed0ae579ed909c239ad2f1e10b2b00d8eed80654874b1641cf6aa5fcc09dc74b"
        }
    },
    "previousStable": {
        "version": "2.0.18",
        "tag": "2.0.18",
        "status": "superseded",
        "publishedAt": "2026-09-22T05:30:17Z",
        "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.18"
    },
    "candidate": {
        "version": "2.0.20",
        "channel": "stable",
        "status": "reserved",
        "sourceBranch": "release/2.0.20",
        "sourceCommit": "20a7981",
        "assetSourceCommit": "20a7981",
        "deviceAcceptance": "pending-desktop",
        "assets": [
            "main.js",
            "manifest.json",
            "manifest-beta.json",
            "styles.css",
            "versions.json"
        ],
        "assetSha256": {
            "main.js": "c7e8d8fb436ae313f3b745e001f302afb35030a16b34a137cd995efb71d89f40",
            "manifest.json": "d2292aacd99b1fc89536839a2e698cf1c9440c841d0d78cc1e7db9b25e94299f",
            "manifest-beta.json": "d2292aacd99b1fc89536839a2e698cf1c9440c841d0d78cc1e7db9b25e94299f",
            "styles.css": "cbc4e6da4f47579ce5158a75e5e96c275f107423b3b201c296d509aafd6f03fa",
            "versions.json": "b199aee1518f7df551bca44f23e50b9bb90af403d1597e05d8f71b0a38b38ba3"
        }
    },
    "notes": [
        "2.0.5 has a repository tag but no GitHub Release, so it is not usable as a BRAT-installable rollback path. The effective rollback target is 2.0.4.",
        "This file was corrected on 2026-09-16 against the GitHub API; it had been left at the 1.0.75 era and no longer described the published state.",
        "2.0.6 was published on 2026-09-16 by fast-forwarding main to the reviewed source commit 0a7dd4d, which triggers .github/workflows/publish-release-assets.yml. The workflow created the tag and the GitHub Release and marked it as a pre-release, so the stable channel still resolves to 2.0.4.",
        "Asset naming: the five BRAT_ASSETS entries recorded here (main.js, manifest.json, manifest-beta.json, styles.css, versions.json) are the versioned repository files that must stay aligned and hashed. The GitHub Release itself carries the three files BRAT actually downloads (main.js, manifest.json, styles.css); manifest-beta.json and versions.json are consumed from the repository tree. This distinction was previously unstated and made the ledger look inconsistent with the published asset list.",
        "docs/releases/osyc-2.0.6-launch-baseline.md remains a dated snapshot of 15 September 2026 and is deliberately not rewritten. Its plug-in publication gate is now met, but its backend gates are still open: a versioned backend candidate with the diagnostics/email contracts, backend tests, health/runtime-info/zero-cost dry-run, the four low-cost acceptance samples, and desktop plus mobile UI acceptance.",
        "2.0.7 was published on 2026-09-16 by fast-forwarding main to the reviewed commit 1fc9006 (6457aaa..1fc9006), which triggered publish-release-assets.yml. The workflow created tag 2.0.7 and the GitHub Release OsyC 2.0.7 with prerelease=true and target_commitish=1fc9006; the three uploaded assets match the local release-info.json byte for byte. 2.0.4 and 2.0.6 were left untouched.",
        "WARNING: GET /releases/latest returned 2.0.0 on 2026-09-16 (checked twice) even though 2.0.4 is newer and is not a prerelease. A BRAT stable (non-beta) install can therefore resolve to 2.0.0 rather than 2.0.4. Confirm on a device and then re-point latest to 2.0.4, or install by explicit version. This is a release-captain action and requires a credential that is not available in this environment.",
        "2.0.8 carries three fixes: the settings jump (openObsidianSettings() called open() before openTabById(), and the account modal passed a LiveSync settings *group* id where Obsidian expects a tab id — the group objects carry no id at all); the colour preset (toAppearance() wrote `preset` into `colourPreset`, so any non-theme typography preset silently applied that preset's hard-coded palette and the chat surface stopped following the Obsidian theme); and the sync provisioning gate (a first activation returned an empty setup_uri while provisioning was still pending, which the plugin reported as a hard failure).",
        "2.0.8 was published on 2026-09-17 by fast-forwarding main to the reviewed commit 141ce77 (d780536..141ce77), which triggered publish-release-assets.yml. The workflow created the tag and the GitHub Release as a pre-release, so the stable channel still resolves to 2.0.4. All three release assets matched the locally recorded sha256 values byte-for-byte.",
        "2.0.9 replaces the settings entry itself: every in-plugin settings button (the tools centre, the account modal and the request-open-settings event) now opens a native Obsidian Modal that renders the same OsyC pane. openTabById can only reach this plugin's settings page, and that page opens on a group list rather than on the settings themselves, so the modal is the only route that shows the settings directly. The LiveSync-only remote configuration stays reachable from an explicitly labelled footer entry. The same release promotes the OsyC root group to the first position on the native settings landing page.",
        "2.0.9 was published on 2026-09-17 by fast-forwarding main to the reviewed commit 354de02 (5329939..354de02), which triggered publish-release-assets.yml. The workflow created tag 2.0.9 and the GitHub Release with the Pre-release badge, so the stable channel was not changed (it is still intended to be 2.0.4; see the existing warning that GET /releases/latest returns 2.0.0). All three release assets matched the locally recorded sha256 values byte-for-byte.",
        "2.0.9 was promoted to stable on 2026-09-17 by a GitHub Releases API PATCH (prerelease=false, make_latest=true) using a maintainer-supplied credential. GET /releases/latest now resolves to 2.0.9 instead of 2.0.0, which closes the long-standing warning recorded on 2026-09-16.",
        "The promotion was first attempted with .github/workflows/release.yml (Publish selected release, tag=2.0.9, prerelease=false). Run #1 failed in 'Validate the selected release' with `Could not find '## 2.0.9' in updates.md.` The tag cannot be moved, so that route was unavailable for 2.0.9; release notes were added for 2.0.7/2.0.8/2.0.9 afterwards so the repository tool works from 2.0.10 onwards.",
        "Correction to earlier notes: \"the stable channel still resolves to 2.0.4\" described the Obsidian community directory listing, not what an installed client can receive. Obsidian determines the latest version from `manifest.json` in the repository default branch (and consults `versions.json` for compatibility), then downloads the assets of the GitHub release whose tag matches that version; the GitHub pre-release badge does not gate client updates. The badge only affects which releases the community directory ingests, which is why its listing page showed 2.0.4 while clients could already update past it.",
        "The Obsidian community directory has its own ingest cadence. The 2.0.9 release became non-prerelease on 2026-09-17, but the directory listing (Current version / Review scan target) still showed 2.0.4 immediately afterwards and is expected to catch up on its next scan.",
        "2.0.9 was REMOVED from the Obsidian community directory on 2026-09-17. The mirror commit b6b66a2c (06:43Z) dropped osyc from community-plugins.json because manifest.json on the default branch already named 2.0.9 while the only 2.0.9 release was still a pre-release, and the directory matches published releases only. The release was promoted at 11:12Z, and the listing is restored by the directory scan (the developer dashboard showed the review as pending on 2.0.9).",
        "2.0.10 is reserved to publish with a normal (non-prerelease) release, which is what the directory requires. It carries the terminal progress cleanup (terminal status hides every progress phase, and the heartbeat merge keeps only its newest tick) and the release-channel guard: publish-release-assets.yml now creates a published release on a push and refuses to create a pre-release from the default branch, and verify-release-channel.yml checks the manifest/release invariant hourly.",
        "2.0.10 was published on 2026-09-17 by fast-forwarding main to fdbfdec (43c0b95..fdbfdec). The release-channel guard made the workflow create 2.0.10 as a normal release on the push itself, so the manifest version and a published release agreed from the first second. All three release assets matched release-info.json byte for byte, and GET /releases/latest resolves to 2.0.10.",
        "Publishing 2.0.10 also restored the Obsidian community directory listing that the 06:43Z mirror commit removed. Desktop search found osyc again within minutes of the release; mobile follows on its own directory refresh.",
        "2.0.11 carries the sync-activation fix (the setup URI payload has no liveSync, so the replicator never opened), a load-time repair for devices already activated under that bug, the removal of the estimated credit display, and the left alignment of the session list rows. The directory listing for 2.0.10 was restored on 2026-09-17.",
        "2.0.11 was published on 2026-09-18 as a normal (non-prerelease) release rather than a pre-release, so the manifest version, the tag 2.0.11 at the reviewed cut commit 0a2ff89 and a published GitHub Release agreed; GET /releases/latest resolves to 2.0.11.",
        "2.0.12 carries the activation reroute (activation now rewrites the remote configuration profile named by activeConfigurationId together with the top-level couchDB_* keys, because LiveSync 2.x overlays the active profile over the top level and a top-level-only write was reverted by the old endpoint on a device that already had a profile), pins remoteType to CouchDB during activation so a legacy minio/p2p value cannot route the replicator to the wrong remote, and presets the official service address https://api4.sacu3.cn while migrating the historical official endpoints so a fresh install can activate after pasting a card key.",
        "2.0.13 is reserved by the branch release/2.0.13 at cut commit 5f86f45; it is unpublished. The candidate carries no tag, releaseUrl or publishedAt on purpose: utils/verify-osyc-collaboration.mjs treats a candidate without a tag as a reserved line ahead of the stable release, so the reviewed tree may carry 2.0.13 while currentStable still names 2.0.11. Publication stays with the release-captain (featureAgentsMayPublish is false) and is the usual fast-forward of main, which makes publish-release-assets.yml create 2.0.13 as a normal release.",
        "2.0.13 is a build-hygiene release: it removes two unnecessary type assertions (planCouchDbRemoteConfigurationReroute in src/osyc/features/AIAgent/livesyncPatch.ts and the currentSettings() call in src/osyc/serviceFeatures/useAIAgentUI.ts), which returns eslint to 0 errors with the nine pre-existing warnings unchanged. There is no runtime change; the five versioned assets were rebuilt and re-hashed, and tsc, svelte-check, the iOS 15 compatibility check and 136 unit-test files / 1019 tests all passed.",
        "2.0.13 was re-cut on 2026-09-20 as commit 2ec9368, before any publication, to carry three changes on top of 5f86f45: the recharge entry in the AI Agent account dialog (integer-credit card keys, with the campaign bonus reported back), email sign-in and card binding in the same dialog (a real flow that replaces the disabled preview; the binding is authorised by the server-issued account session token rather than a client-chosen device identifier, and the session stays in memory), and the brand unification (OsyC-logo.png at the repository root with derived sizes in assets/brand/, documented in docs/BRAND_ASSETS.zh.md, and user-facing signatures that read OsyC again). The earlier note describing 2.0.13 as a no-runtime-change build-hygiene release therefore describes commit 5f86f45 only; the candidate now names 2ec9368 and release-info.json records the rebuilt main.js fingerprint 10c9563ae4eb1e465c1399523e3efe5356701922d87881a8cde7965d02dc48e0. The other four versioned assets are unchanged (manifest.json and manifest-beta.json a3af3f53, styles.css 63af2cd2, versions.json c319d88e). The gate before publication was tsc, the review-hygiene icon whitelist and the full unit suite on the 2ec9368 tree.",
        "Ledger state on 2026-09-20: currentStable still names 2.0.11 even though 2.0.12 was published as a normal release on 2026-09-18T04:10:43Z. The 2.0.12 record, including its published asset list and all five fingerprints, was moved to previousCandidate so that reserving 2.0.13 would not discard it. Promoting currentStable to 2.0.12 remains a release-captain action.",
        "2.0.13 was cut again on 2026-09-20 as commit c555adf on release/2.0.13, superseding 2ec9368 as the candidate fingerprint. This cut merges the Pro independent sync-space client entry (explicit activation from the account dialog plus a read-back self-check before success is reported), the activation-path repair that creates a legacy-couchdb remote database when no profile exists, the mobile first-run keyboard fix, the extended release gates and docs, and another agent's i18n and BYOK changes (Doctor-dialog placeholders restored across es/ru/zh with a placeholder-contract sentinel test, and the BYOK entry hidden in the account dialog while the production backend does not honour devices.byo_key). release-info.json records the rebuilt main.js fingerprint 8eccab6a93232e37b8441fe78c4247d72423ff601d2ba073f0b43796e4280233 and the new styles.css fingerprint d96d205b051ce2868dace8b7698958444cf55b38290f349baea148541ff7f4cb; manifest.json, manifest-beta.json and versions.json are unchanged (a3af3f53, a3af3f53, c319d88e). The gate on the c555adf tree was tsc --noEmit --skipLibCheck (0 errors) and the full unit suite (141 files / 1062 tests green). Ledger drift still stands and needs release-captain confirmation: currentStable still names 2.0.11 while 2.0.12 was published as a normal release on 2026-09-18T04:10:43Z. Promoting currentStable to 2.0.12, and every publication action, remain with the release-captain.",
        "2.0.13 was published on 2026-09-20T10:06:46Z by fast-forwarding main from b7fd1e3 to a50eb4b, which triggered publish-release-assets.yml run 35504140301 (run #82, conclusion success). The workflow created tag 2.0.13 and the GitHub Release OsyC 2.0.13 as a normal (non-prerelease) release, so the release-channel guard held and no pre-release promotion was needed. GET /releases/latest resolves to 2.0.13 and manifest.json on the default branch reads 2.0.13. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js 8eccab6a, manifest.json a3af3f53, manifest-beta.json a3af3f53, styles.css d96d205b, versions.json c319d88e.",
        "2.0.13 contains the Pro independent sync-space client entry (explicit activation from the account dialog with a read-back self-check before success is reported), the activation-path repair that creates a legacy-couchdb remote database when no profile exists, the mobile first-run keyboard fix, and the extended release gates and closure documentation.",
        "Note on the 2.0.13 tag target: release-info.json names the cut commit c555adf, but the publisher tags the pushed default-branch HEAD, so tag 2.0.13 points at a50eb4b, the release commit that records the c555adf fingerprint. The five asset blobs are identical in c555adf and a50eb4b; only release-info.json and this ledger differ between them. This differs from the 2.0.11/2.0.12 pattern, where the tag sat exactly on the reviewed cut commit, and it is recorded here so the next release does not read it as drift.",
        "2.0.14 is reserved by the branch release/2.0.14 at cut commit fec3b39; it is unpublished and the candidate intentionally carries no tag, releaseUrl or publishedAt. It carries the account-dialog LiveSync panel with milestone diagnostics and one-click fetch/push/accept/rebuild actions, the direct-only endpoint migration (api.sacu3.cn and osyctest.sacu3.cn are LEGACY migration sources only and never runtime fallbacks), the livesyncPatch reconciliation with the activation read-back self-check and the no-profile legacy-couchdb creation, the start-up self-heal for the 2.0.13 customChunkSize=60 defect, expired-Pro read-only retention (server contract HTTP 200 with namespace.status=expired and read_only=true), the tools-centre email and Pro entries that share osycAccountSections.ts, and the carried-forward mobile first-run keyboard fix. The five versioned assets were rebuilt and re-hashed after the cut: main.js b650c5da, manifest.json and manifest-beta.json ad14789e, styles.css cbc4e6da, versions.json 398b27c4; release-info.json names the cut commit fec3b39, and the rebuilt main.js is committed separately as 4d6aac3, so the tag will point at the pushed HEAD rather than at the cut commit. The gate on the cut tree was tsc --noEmit --skipLibCheck (0 errors), the full unit suite (144 files / 1120 tests green) and the sync acceptance self-test (8/8 OK).",
        "Known boundaries recorded with the 2.0.14 reservation: the start-up self-heal corrects exactly customChunkSize === 60 back to 0 and leaves 0 or any other user-set value untouched (the known PREFERRED=60 boundary is documented in docs/LIVESYNCPATCH-RECONCILIATION.zh.md); Cloudflare-tunnelled endpoints are migration sources only, and no Cloudflare candidate may enter the runtime fallback list (DIRECT_FALLBACK_SERVICE_URLS stays empty); and the Pro quota surface reports usage only and does not refuse a request.",
        "2.0.14 was published on 2026-09-21T02:05:55Z by fast-forwarding main from 2c5f83a to d115d72, which triggered publish-release-assets.yml run 35553006601 (run #84, conclusion success). The workflow created tag 2.0.14 and the GitHub Release OsyC 2.0.14 as a normal (non-prerelease) release, so no pre-release promotion was needed. GET /releases/latest resolves to 2.0.14 and manifest.json on the default branch reads 2.0.14. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js b650c5da, manifest.json ad14789e, manifest-beta.json ad14789e, styles.css cbc4e6da, versions.json 398b27c4.",
        "Note on the 2.0.14 tag target: release-info.json names the cut commit fec3b39, and the rebuilt main.js was committed separately as 4d6aac3 before the release-info (aa741ce) and ledger (d115d72) commits, so tag 2.0.14 points at the pushed default-branch HEAD d115d72 rather than at the cut commit. This follows the 2.0.13 pattern recorded above.",
        "Ledger promotion on 2026-09-21: currentStable moves from 2.0.13 (now previousStable, superseded, tag 2.0.13 at a50eb4b) to 2.0.14, and the reserved 2.0.14 candidate line is consumed. The 2.0.12 record stays in previousCandidate.",
        "2.0.15 is reserved by the branch release/2.0.15 at cut commit 1fbe3e0; the rebuilt main.js is committed separately as bb0322f, so tag 2.0.15 will point at the pushed default-branch HEAD rather than at the cut commit (the 2.0.13/2.0.14 pattern). The candidate intentionally carries no tag, releaseUrl or publishedAt. It closes the card-key -> email binding loop (the bind-purpose code on POST /api/email/send-code and the POST /api/account/bind-email consumer, which issues no device token and does not rewrite the local settings.token), separates the two opposite binding directions in the shared tools-centre email block (email -> card key and card key -> email, with bind-purpose and login codes not interchangeable), states that the email session is in-memory only and needs fresh verification after a restart, prefers the server-supplied detail in the email error copy (control characters folded, 240-character cap, status-code fallback retained), clears the two 2.0.14 lint errors (no-base-to-string on the endpoint-failover log call and no-unnecessary-type-assertion on the profile read after the isRecord() guard) with the nine pre-existing warnings unchanged, makes the start-up self-heal log name the repair actually applied (the LiveSync master switch and/or customChunkSize), and adds the 2.0.15 device acceptance checklist docs/DEVICE-ACCEPTANCE-2.0.15.zh.md.",
        "The five versioned assets were rebuilt and re-hashed after the 2.0.15 cut: main.js cc7c40d4, manifest.json and manifest-beta.json 63bf8cf3, styles.css cbc4e6da (unchanged from 2.0.14), versions.json dd3d28e5. release-info.json names the cut commit 1fbe3e0. The candidate gate on the cut tree was tsc --noEmit --skipLibCheck (0 errors), eslint (0 errors / 9 warnings), svelte-check (0 errors / 0 warnings), tsc-check:apps (0 errors), the full unit suite (146 files / 1144 tests green) and the sync acceptance self-test (8/8 OK). Build products follow in a separate commit.",
        "Known boundaries recorded with the 2.0.15 reservation: bind-email authorises only the currently activated card key and leaves the local identity unchanged; a bind-purpose verification code is not interchangeable with a login code; the email session is deliberately in-memory, so a restart requires fresh verification; currentStable.deviceAcceptance remains pending-desktop until the 2.0.15 device checklist passes; and lint still reports the nine pre-existing warnings unchanged.",
        "2.0.15 was published on 2026-09-21T02:41:59Z by fast-forwarding main from da37be1 to 88a218e, which triggered publish-release-assets.yml run 35555019299 (run #86, conclusion success). The workflow created tag 2.0.15 and the GitHub Release OsyC 2.0.15 as a normal (non-prerelease) release, so no pre-release promotion was needed. GET /releases/latest resolves to 2.0.15 and manifest.json on the default branch reads 2.0.15. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js cc7c40d4, manifest.json 63bf8cf3, manifest-beta.json 63bf8cf3, styles.css cbc4e6da, versions.json dd3d28e5.",
        "Note on the 2.0.15 tag target: release-info.json names the cut commit 1fbe3e0, and the rebuilt main.js was committed separately as bb0322f before the release-info (4adbf1b) and ledger reservation (88a218e) commits, so tag 2.0.15 points at the pushed default-branch HEAD 88a218e rather than at the cut commit. This follows the 2.0.13/2.0.14 pattern.",
        "Ledger promotion on 2026-09-21: currentStable moves from 2.0.14 (now previousStable, superseded, tag 2.0.14 at d115d72) to 2.0.15, and the reserved 2.0.15 candidate line is consumed. The 2.0.13 record that previously sat in previousStable is preserved together with 2.0.12 in previousCandidate, which is now a list of historical records ordered newest first.",
        "2.0.16 is reserved by the branch release/2.0.16 at cut commit fa2722d; the rebuilt main.js is committed separately as 441767d, so tag 2.0.16 will point at the pushed default-branch HEAD rather than at the cut commit (the 2.0.13/2.0.14/2.0.15 pattern). The candidate intentionally carries no tag, releaseUrl or publishedAt. 2.0.16 is the launch-blocker fix release and carries three plugin-side fixes: the fresh-install boot fallback (useAIAgentUI now unconditionally calls agent.configure(resolveServiceUrl(undefined), \"\") on the persisted-configuration fallback path, so a brand-new install resolves apiBase to the single official direct entry point https://api4.sacu3.cn instead of keeping an empty string that short-circuited activate() with 尚未配置 OsyC 服务地址 before any request left the client, while a saved configuration still overwrites the fallback); the corrected activation error copy with server-detail priority (describeError maps 401 to 卡密无效或登录已失效，请重新输入卡密, 402 to 卡密已过期，请续费 and the new 426 branch to 当前插件版本过旧，请更新插件 instead of the generic 服务端错误（426）, activate() returns describeResponseError(res, res.status) on status >= 400 so a server-supplied detail is echoed first, and the new exported sanitiseServerDetail unifies C0/C1 control-character stripping, whitespace folding and the 240-character cap with readEmailErrorDetail reusing it); and the card-key normalisation (the new exported normalizeCardKey strips all whitespace and upper-cases, and activate, recharge, bindCardToEmail and the email-login res.card_key all use it, with the same normalised value passed to applySetupUri so the setup URI is decrypted with the key that was actually sent; the interface is unchanged and Pro passphrase values are deliberately not normalised). The server-side Pro route gap recorded as the other launch blocker is not addressed by this plugin release.",
        "The five versioned assets were rebuilt and re-hashed after the 2.0.16 cut: main.js 8f189d88, manifest.json and manifest-beta.json b3b72f0f, styles.css cbc4e6da (unchanged from 2.0.15), versions.json 96d17c5a. release-info.json names the cut commit fa2722d. The candidate gate on the cut tree was tsc --noEmit --skipLibCheck (0 errors), eslint (0 errors / 9 pre-existing warnings), svelte-check (0 errors / 0 warnings), tsc-check:apps (0 errors), the full unit suite on the threads pool (147 files / 1157 tests green) and the sync acceptance self-test (8/8 OK). Build products follow in a separate commit.",
        "Known boundaries recorded with the 2.0.16 reservation: the first-run fallback is covered by a Node-level behaviour test and a source regression guard, not by a real-device cold start with no .obsidian/livesync-aiagent.json; the real server 401/402/426 response bodies (the detail field name and wording) were not checked locally and are taken from the buyer acceptance record; the 426 minimum-version guidance is free text echoed as-is and is not parsed; the server-side Pro route that is the other launch blocker is fixed outside this repository; and lint still reports the nine pre-existing warnings unchanged.",
        "2.0.16 was published on 2026-09-21T03:31:46Z by fast-forwarding main from 2185a54 to 1c87af5, which triggered publish-release-assets.yml run 35557812312 (run #88, conclusion success). The workflow created tag 2.0.16 and the GitHub Release OsyC 2.0.16 as a normal (non-prerelease) release, so no pre-release promotion was needed. GET /releases/latest resolves to 2.0.16 and manifest.json on the default branch reads 2.0.16. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js 8f189d88, manifest.json b3b72f0f, manifest-beta.json b3b72f0f, styles.css cbc4e6da, versions.json 96d17c5a.",
        "Note on the 2.0.16 tag target: release-info.json names the cut commit fa2722d, and the rebuilt main.js was committed separately as 441767d before the release-info (326b4fa) and ledger reservation (1c87af5) commits, so tag 2.0.16 points at the pushed default-branch HEAD 1c87af5 rather than at the cut commit. This follows the 2.0.13/2.0.14/2.0.15 pattern.",
        "Ledger promotion on 2026-09-21: currentStable moves from 2.0.15 (now previousStable, superseded, tag 2.0.15 at 88a218e) to 2.0.16, and the reserved 2.0.16 candidate line is consumed. The 2.0.14 record that previously sat in previousStable is preserved together with 2.0.13 and 2.0.12 in previousCandidate, which remains a list of historical records ordered newest first.",
        "2.0.17 is reserved by the branch release/2.0.17 at cut commit 898c347; the rebuilt main.js is committed separately as 42e7651, so tag 2.0.17 will point at the pushed default-branch HEAD rather than at the cut commit (the 2.0.13/2.0.14/2.0.15/2.0.16 pattern). The candidate intentionally carries no tag, releaseUrl or publishedAt. 2.0.17 carries two plugin-side changes. First, the email-login tier and entitlement label repair: the server's GET /api/status now returns plan and entitlements alongside the existing fields (the same source as /api/activate), and refreshStatus() writes them into state.plan / state.entitlements; a field that is missing or invalid keeps the previous value instead of falling back to base, so an email code login can no longer downgrade an already-activated member or Pro account to the base label, and the new CmdAIAgent.tierLabel spec locks the mapping with six cases. Second, the redacted diagnostics capability: osycLogger exports redactSensitiveText and keeps a 20-entry API-failure ring buffer; the diagnostics payload carries account_summary, livesync_summary (masked endpoint, node-id prefix, accepted flag, last pull/push and a settings fingerprint over customChunkSize, hashAlg, chunkSplitterVersion and remoteType) and api_failures; the diagnostics_log limit rises 600 -> 8000 characters; activation, setup-URI apply plus read-back self-check, pull/push, milestone accept, API 4xx/5xx, the Pro namespace and email login/bind now log through osycLogger; the tools centre gains a manual upload-redacted-diagnostics entry; and AIAgentTaskCard registers the synthetic manual task status in STATUS_META so the card type-checks.",
        "2.0.17 含诊断能力，用于定位客户端配置不匹配。",
        "The five versioned assets were rebuilt and re-hashed after the 2.0.17 cut: main.js f67ccb75, manifest.json and manifest-beta.json 03dbdc32, styles.css cbc4e6da (unchanged from 2.0.16), versions.json d2fe3604. release-info.json names the cut commit 898c347. The candidate gate on the cut tree was tsc --noEmit --skipLibCheck (0 errors), eslint (0 errors / 9 pre-existing warnings), svelte-check (0 errors / 0 warnings), tsc-check:apps (0 errors), the full unit suite on the threads pool (150 files / 1212 tests green) and the sync acceptance self-test (8/8 OK). Build products follow in a separate commit.",
        "Known boundaries recorded with the 2.0.17 reservation: the svelte-check gate caught a defect in the diagnostics commit itself (the new synthetic manual AITaskStatus value was not registered in AIAgentTaskCard's STATUS_META, which tsc --noEmit cannot see because it ignores .svelte files); the fix reuses a glyph already present in that table, was made on the source branch codex/diagnostics-2.0.17 (now fe87dba) and was re-integrated before this cut. docs/sellerDocsHygiene.unit.spec.ts is carried by this release and is stricter than the current documentation requires (it forbids every yen sign across docs/ and updates.md outside a small allowlist); it passes on this tree but must be relaxed deliberately in a later change, not edited during a release. The diagnostics payload and the settings fingerprint are covered by unit tests, not by a live read-back from the production service, and the server-side /api/status plan and entitlements fields are taken as already deployed. lint still reports the nine pre-existing warnings unchanged.",
        "2.0.17 was published on 2026-09-21T04:50:10Z by fast-forwarding main from e658f0b to 0ce12bb, which triggered publish-release-assets.yml run 35562386987 (run #90, conclusion success). The workflow created tag 2.0.17 and the GitHub Release OsyC 2.0.17 as a normal (non-prerelease) release, so no pre-release promotion was needed. GET /releases/latest resolves to 2.0.17 and manifest.json on the default branch reads 2.0.17. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js f67ccb75, manifest.json 03dbdc32, manifest-beta.json 03dbdc32, styles.css cbc4e6da, versions.json d2fe3604.",
        "Note on the 2.0.17 tag target: release-info.json names the cut commit 898c347, and the rebuilt main.js was committed separately as 42e7651 before the release-info (4a3c409) and ledger reservation (0ce12bb) commits, so tag 2.0.17 points at the pushed default-branch HEAD 0ce12bb rather than at the cut commit. This follows the 2.0.13/2.0.14/2.0.15/2.0.16 pattern.",
        "Ledger promotion on 2026-09-21: currentStable moves from 2.0.16 (now previousStable, superseded, tag 2.0.16 at 1c87af5) to 2.0.17, and the reserved 2.0.17 candidate line is consumed. The 2.0.15 record that previously sat in previousStable is preserved together with 2.0.14, 2.0.13 and 2.0.12 in previousCandidate, which remains a list of historical records ordered newest first.",
        "2.0.17 shipped after the svelte-check gate caught a defect in the diagnostics commit on its source branch (the new synthetic manual AITaskStatus value was missing from AIAgentTaskCard's STATUS_META, and tsc --noEmit does not scan .svelte files); the source branch codex/diagnostics-2.0.17 was amended to fe87dba, the integration branch was rebuilt from e658f0b, and all six candidate gates then passed before the cut.",
        "2.0.18 is reserved by the branch release/2.0.18 at cut commit 2acee51; the rebuilt main.js is committed separately as 03263b1, so tag 2.0.18 will point at the pushed default-branch HEAD rather than at the cut commit (the 2.0.13/2.0.14/2.0.15/2.0.16/2.0.17 pattern). The candidate intentionally carries no tag, releaseUrl or publishedAt. The tree integrates exactly three local lines on top of 2.0.17 (391268c): the sync-configuration self-heal with the always-visible 修复同步配置 entry and the 重新激活卡密 move out of the default-collapsed fold (f8ee8f4), the email-identity contract completion (e819721) and the engineering foundation gate-all.mjs / release-closure checklist / foundation index (be39ab5). 本次不含支付相关改动：不并入充值入口 4207c4f，不含收款码或订单 UI。",
        "The 2.0.18 tree also narrows the seller/buyer documentation gate (2acee51, cherry-picked to codex/foundation-2.0.18 as bb58f0b) and closes the D4 over-broad-gate todo. The rule now fails only a real money-to-credit/quota mapping (12 元额度, ¥12 额度, 1 积分 = 0.015 元, 折合/等值/相当于/换算/兑换 + an amount, model_quota 12.0 元, pro_grant_yuan=18.00 x credits_per_yuan); a policy or technical note that mentions 人民币 while saying the mapping is not written is compliant, so the correct docs/FOUNDATION-INDEX.zh.md note passes; and the buyer-facing listing and acceptance documents keep the strict price-free form (no yen sign, no 人民币, no number+元, no RMB). The INTERNAL_DOC_ALLOWLIST is removed because the precise rule no longer needs it. The gate patch is carried in this release because the foundation document would otherwise fail the gate its own branch introduced; the same spec commit is applied to codex/foundation-2.0.18, which is also 6/6 green.",
        "The five versioned assets were rebuilt and re-hashed after the 2.0.18 cut: main.js f634d9e4, manifest.json and manifest-beta.json 99bae65c, styles.css cbc4e6da (unchanged from 2.0.17), versions.json f6820fdf. release-info.json names the cut commit 2acee51. The candidate gate on the cut tree was node scripts/gate-all.mjs 6/6 PASS, exit 0: tsc --noEmit --skipLibCheck (0 errors), eslint (0 errors / 9 pre-existing warnings), svelte-check (0 errors / 0 warnings), tsc-check:apps (0 errors), the full unit suite on the threads pool (150 files / 1234 tests green) and the sync acceptance self-test (8/8 OK). Build products follow in a separate commit.",
        "本次不含支付相关改动：2.0.18 的 origin/main..HEAD diff 中零 osycRechargeSection.ts、OsycRechargeModal.ts、rechargeEntry.ts 等支付/收款码/订单 UI 文件；codex/recharge-entry-2.0.18 (4207c4f) 一律未并入。",
        "Ledger promotion on 2026-09-22: currentStable moves from 2.0.17 (now previousStable, superseded, tag 2.0.17 at 0ce12bb) to 2.0.18, and the reserved 2.0.18 candidate line is consumed. The 2.0.16 record that previously sat in previousStable is preserved together with 2.0.15, 2.0.14, 2.0.13 and 2.0.12 in previousCandidate, which remains a list of historical records ordered newest first.",
        "2.0.18 was published on 2026-09-22T05:30:17Z by fast-forwarding main from 391268c to 96b4fef, which triggered publish-release-assets.yml run 35690934232 (conclusion success). The workflow created tag 2.0.18 (a lightweight tag pointing at 96b4fef) and the GitHub Release OsyC 2.0.18 as a normal (non-prerelease) release, so no pre-release promotion was needed. GET /releases/latest resolves to 2.0.18 and manifest.json on the default branch reads 2.0.18. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js f634d9e4, manifest.json 99bae65c, manifest-beta.json 99bae65c, styles.css cbc4e6da, versions.json f6820fdf.",
        "Note on the 2.0.18 tag target: release-info.json names the cut commit 2acee51, and the rebuilt main.js was committed separately as 03263b1 before the release-info (dd2a6b0) and ledger reservation (96b4fef) commits, so tag 2.0.18 points at the pushed default-branch HEAD 96b4fef rather than at the cut commit. This follows the 2.0.13/2.0.14/2.0.15/2.0.16/2.0.17 pattern.",
        "2.0.18 发布时仍不含支付相关改动：未并入 codex/recharge-entry-2.0.18 (4207c4f)。",
        "2.0.19 is reserved by the branch release/2.0.19 at cut commit 334de7f; the rebuilt main.js is committed separately as ce61bfb, so tag 2.0.19 will point at the pushed default-branch HEAD rather than at the cut commit (the 2.0.13/2.0.14/2.0.15/2.0.16/2.0.17/2.0.18 pattern). The candidate intentionally carries no tag, releaseUrl or publishedAt. 2.0.19 carries the cross-device tweak auto-alignment fix (f3bf628, cherry-picked as f998b69): planTweakAlignment() compares all 18 TweakValuesShouldMatchedTemplate keys after merging TweakValuesDefault and mirroring isObjectDifferent(..., true), and on an official managed remote (official endpoint domain or an osyc_sync_ account) it auto-aligns every diff, encrypt included, through LiveSync own use-remote branch (extractObject(TweakValuesTemplate, preferred) -> saveSettingData -> setPreferredRemoteTweakSettings -> $fetchLocal() when a rebuild is needed), at four points (start-up, after activation, when the plugin panel or account dialog opens, and before every pull), with no dialog and no confirmation; self-hosted remotes still only warn and require an explicit confirmation, and FORBIDDEN_KEYS is reused so no auto-align path can write a passphrase or credential. 诊断字段新增 tweak_diff / remote_preferred_status / tweak_settings_mismatched。本次只含同步参数自动对齐修复与诊断字段，不含任何支付/充值改动。",
        "The five versioned assets were rebuilt and re-hashed after the 2.0.19 cut: main.js 1b89822a, manifest.json and manifest-beta.json 770f98c0, styles.css cbc4e6da (unchanged from 2.0.18), versions.json ed0ae579. release-info.json names the cut commit 334de7f. The candidate gate on the cut tree was node scripts/gate-all.mjs 6/6 PASS, exit 0: tsc --noEmit --skipLibCheck (0 errors), eslint (0 errors / 9 pre-existing warnings), svelte-check (0 errors / 0 warnings), tsc-check:apps (0 errors), the full unit suite on the threads pool (151 files / 1261 tests green) and the sync acceptance self-test (8/8 OK). Build products follow in a separate commit.",
        "本次不含支付相关改动：2.0.19 的 origin/main..HEAD diff 中零 osycRechargeSection.ts、OsycRechargeModal.ts、rechargeEntry.ts 等支付/收款码/订单 UI 文件；codex/recharge-entry-2.0.18 (4207c4f) 一律未并入。",
        "Ledger promotion on 2026-09-22: currentStable moves from 2.0.18 (now previousStable, superseded, tag 2.0.18 at 96b4fef) to 2.0.19, and the reserved 2.0.19 candidate line is consumed. The 2.0.17 record that previously sat in previousStable is preserved together with 2.0.16, 2.0.15, 2.0.14, 2.0.13 and 2.0.12 in previousCandidate, which remains a list of historical records ordered newest first.",
        "2.0.19 was published on 2026-09-22T06:15:40Z by fast-forwarding main from e184f0c to 92514bc, which triggered publish-release-assets.yml run 35694028272 (conclusion success). The workflow created tag 2.0.19 (a lightweight tag pointing at 92514bc) and the GitHub Release OsyC 2.0.19 as a normal (non-prerelease) release, so no pre-release promotion was needed. GET /releases/latest resolves to 2.0.19 and manifest.json on the default branch reads 2.0.19. All five versioned assets were downloaded and hashed after publication and match release-info.json byte for byte: main.js 1b89822a, manifest.json 770f98c0, manifest-beta.json 770f98c0, styles.css cbc4e6da, versions.json ed0ae579.",
        "Note on the 2.0.19 tag target: release-info.json names the cut commit 334de7f, and the rebuilt main.js was committed separately as ce61bfb before the release-info (3b771ba) and ledger reservation (92514bc) commits, so tag 2.0.19 points at the pushed default-branch HEAD 92514bc rather than at the cut commit. This follows the 2.0.13/2.0.14/2.0.15/2.0.16/2.0.17/2.0.18 pattern.",
        "2.0.19 发布时仍不含支付相关改动：未并入 codex/recharge-entry-2.0.18 (4207c4f)；该版本只含同步参数自动对齐修复与诊断字段。",
        "2.0.20 is reserved by the branch release/2.0.20 at cut commit 20a7981; the rebuilt main.js is committed separately as 4876b5b, so tag 2.0.20 will point at the pushed default-branch HEAD rather than at the cut commit (the 2.0.13/2.0.14/2.0.15/2.0.16/2.0.17/2.0.18/2.0.19 pattern). The candidate intentionally carries no tag, releaseUrl or publishedAt. 2.0.20 carries two changes. First, the interactive conflict-resolution dialog (ConflictResolveModal) previously hard-coded every user-facing string in English, so a device whose interface language is Chinese still showed an English dialog (confirmed against a user iPhone screenshot); every string now resolves through the repository $msg message table, the three keys the read-only comparison pane already requested (Vault and database revision, Vault file, Database revision) are added so they stop falling back to English, and zh plus zh-tw carry the translations, with a unit spec locking all three languages and a source-level rule that no English interface phrase may appear as a literal outside a $msg key. Second, a must-match tweak write gate (tweakBaselineGuard.ts) states the invariant that any value our code writes to one of the 18 TweakValuesShouldMatchedTemplate keys must be the commonlib template baseline or the remote PREFERRED alignment; it covers the setup patch, the provisioned-replication repair and the final activation patch output including the profile rewrite, is exercised across all 18 keys with an in-memory customChunkSize=60 negative control, and pins the known usePluginSyncV2 deviation as reported rather than silently allowed. 本次只含中文界面与同步参数写入门禁，不含任何支付/充值改动。",
        "The five versioned assets were rebuilt and re-hashed after the 2.0.20 cut: main.js c7e8d8fb, manifest.json and manifest-beta.json d2292aac, styles.css cbc4e6da (unchanged from 2.0.19), versions.json b199aee1. release-info.json names the cut commit 20a7981. The candidate gate on the cut tree was node scripts/gate-all.mjs 6/6 PASS, exit 0: tsc --noEmit --skipLibCheck (0 errors), eslint (0 errors / 9 pre-existing warnings), svelte-check (0 errors / 0 warnings), tsc-check:apps (0 errors), the full unit suite on the threads pool (153 files / 1277 tests green) and the sync acceptance self-test (8/8 OK). Build products follow in a separate commit.",
        "本次不含支付相关改动：2.0.20 的 origin/main..HEAD diff 中零 osycRechargeSection.ts、OsycRechargeModal.ts、rechargeEntry.ts 等支付/收款码/订单 UI 文件；codex/recharge-entry-2.0.18 (4207c4f) 一律未并入。"
    ],
    "publicationRules": {
        "singleWriter": "release-captain",
        "immutableTags": true,
        "forcePush": false,
        "featureAgentsMayPublish": false,
        "featureAgentsMayPublishTestPackages": true,
        "testPackage": {
            "mustBePrerelease": true,
            "requiresReservedVersion": true,
            "requiresExactFiveAssets": true,
            "stablePromotionOwner": "release-captain"
        },
        "bratStableValidationRequired": true
    },
    "previousCandidate": [
        {
            "version": "2.0.17",
            "tag": "2.0.17",
            "status": "superseded",
            "publishedAt": "2026-09-21T04:50:10Z",
            "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.17"
        },
        {
            "version": "2.0.16",
            "tag": "2.0.16",
            "status": "superseded",
            "publishedAt": "2026-09-21T03:31:46Z",
            "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.16"
        },
        {
            "version": "2.0.15",
            "tag": "2.0.15",
            "status": "superseded",
            "publishedAt": "2026-09-21T02:41:59Z",
            "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.15"
        },
        {
            "version": "2.0.14",
            "tag": "2.0.14",
            "status": "superseded",
            "publishedAt": "2026-09-21T02:05:55Z",
            "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.14"
        },
        {
            "version": "2.0.13",
            "tag": "2.0.13",
            "status": "superseded",
            "publishedAt": "2026-09-20T10:06:46Z",
            "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.13"
        },
        {
            "version": "2.0.12",
            "status": "stable-published",
            "channel": "stable",
            "sourceBranch": "codex-activation-remote-config-reroute",
            "stablePromotionOwner": "release-captain",
            "sourceCommit": "5dd2644",
            "assetSourceCommit": "5dd2644",
            "tag": "2.0.12",
            "releaseUrl": "https://github.com/3yesore/OsyC/releases/tag/2.0.12",
            "publishedAt": "2026-09-18T04:10:43Z",
            "deviceAcceptance": "pending-desktop",
            "publishedAssets": [
                "main.js",
                "manifest.json",
                "styles.css"
            ],
            "assetSha256": {
                "main.js": "ef188aa8f353fc715859d750af05fa1c8183b14883e53c6d122464283ecaf114",
                "manifest.json": "51153cdb0b139190ed8bcac8fc418209b295536c0eae151dc3d3f700aae0a0ea",
                "manifest-beta.json": "51153cdb0b139190ed8bcac8fc418209b295536c0eae151dc3d3f700aae0a0ea",
                "styles.css": "63af2cd29cb978990549a1b7cd18624abeea2136b57dda1eed4fa1eae1a6c121",
                "versions.json": "5b7d0ff2bbba52ba2ca44814f03df79cc2b687fb7665dfb2939716f76a12de57"
            }
        }
    ]
}
